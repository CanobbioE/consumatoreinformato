spring.profiles.active=dev

spring.jpa.hibernate.ddl-auto=create

server.servlet.contextPath=/app-service

# 1 day
security.jwt.token.expire-length=86400000
# 1 week
security.jwt.token.regeneration.expire-length=604800000

app.value-scale=10
app.divide-scale=10
app.divide-rounding=HALF_UP


security.jwt.token.secret-key=1212121212

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/uploads
